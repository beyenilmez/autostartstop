name: CI

on:
  push:
    branches: [main]
    paths:
      - '**'
      - '!docs/**'
      - '!*.md'
      - '!LICENSE'
      - '!.github/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main]
    paths:
      - '**'
      - '!docs/**'
      - '!*.md'
      - '!LICENSE'
      - '!.github/**'
      - '.github/workflows/ci.yml'
  workflow_dispatch:
    inputs:
      release:
        description: 'Create a draft GitHub release'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  version:
    name: Extract Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      artifact_name: ${{ steps.version.outputs.artifact_name }}
      bumped: ${{ steps.version.outputs.bumped }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          sparse-checkout: build.gradle.kts
          sparse-checkout-cone-mode: false

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep -oP 'version\s*=\s*"\K[^"]+' build.gradle.kts)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "artifact_name=AutoStartStop-$VERSION.jar" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

          # Check if a tag for this version already exists
          if git ls-remote --tags origin | grep -q "refs/tags/v${VERSION}$"; then
            echo "bumped=false" >> $GITHUB_OUTPUT
            echo "Tag v$VERSION already exists"
          else
            echo "bumped=true" >> $GITHUB_OUTPUT
            echo "Tag v$VERSION does not exist — version was bumped"
          fi

  build:
    name: Build
    needs: version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Java
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@f29f5a9d7b09a7c6b29859002d29d24e1674c884 # v5

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload build artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: plugin-jar-${{ github.run_id }}
          path: build/libs/${{ needs.version.outputs.artifact_name }}
          if-no-files-found: error

  github-release:
    name: GitHub Release
    if: |
      (github.event_name == 'workflow_dispatch' && inputs.release) ||
      (github.event_name == 'push' && needs.version.outputs.bumped == 'true')
    runs-on: ubuntu-latest
    needs: [version, build]
    steps:
      - name: Download artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: plugin-jar-${{ github.run_id }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          tag_name: v${{ needs.version.outputs.version }}
          name: v${{ needs.version.outputs.version }}
          draft: true
          generate_release_notes: true
          files: ${{ needs.version.outputs.artifact_name }}